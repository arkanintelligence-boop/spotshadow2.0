version: '3.8'

services:
  spotify-downloader:
    build: .
    container_name: spotify-downloader
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - HOST=0.0.0.0
    restart: unless-stopped
